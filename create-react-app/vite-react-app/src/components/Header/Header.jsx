const Header = () => {

  //write js logic before return
  const topic = "React";

  return ( 
  <header>
    <h1>Intro to {topic}</h1>
    <p>We are learning {topic}</p>
  </header>
  );
};

export default Header;